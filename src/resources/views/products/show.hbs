<div class="mt-4">
  <div class="row">
    <div class="col-lg-3 text-center mt-4">
      <button class="learn">SEE</button>
    </div>
    <div class="col-lg-9">
      <h2>{{product.name}}</h2>
      <img src="{{product.image}}" alt="" />
      <p>{{product.description}}</p>
    </div>
  </div>
</div>
